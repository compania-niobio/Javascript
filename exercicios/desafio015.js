/* let numeros = [5, 10, 15, 20, 25];

let dobrados = numeros.map(n=> n * 2);
console.log(dobrados); */
 //Desafio
let numeros = [5, 10, 15, 20, 25];
let somas = numeros.map(n=> n +10);
console.log(somas);
